# Rails Example

- Name: Studio Booker
- Framework: Ruby on Rails
- Language: Ruby
- Command: `/example run rails`

Prompt:

```text
Build a Ruby on Rails booking app called Studio Booker for reserving recording rooms. Include rooms, bookings, calendar views, admin management, validation for overlapping reservations, and clean server-rendered UI with modern styling. Generate docs and initialize the app end to end.
```

Expected outcome:

```text
Rails scheduling app with room booking flows, admin CRUD, validations, and end-to-end setup docs.
```
